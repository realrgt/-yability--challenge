name: yability_challenge
description: A new Flutter application.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_modular: ^3.3.1
  flutter_bloc: ^7.0.1
  dartz: ^0.10.0-nullsafety.2
  equatable: ^2.0.3
  hive_flutter: ^1.1.0
  digit_to_word: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  mocktail: ^0.1.4
  flutter_lints: ^1.0.0
  flutter_modular_test: ^1.0.1
  bloc_test: ^8.0.2

flutter:
  uses-material-design: true
